# Example repo

A sample docs repository for Gloss. `design/design.md` is the document under
review; `design/.gloss/design.md/` holds its review log. The dev server
(`npm run server:dev`) serves this repo from an in-memory host so you can hit the
API without a token or network.
